# Example only. Do not apply or commit real values. scripts/k8s_deploy.sh creates
# this Secret from CONTROL_PLANE_ADMIN_TOKEN, GATEWAY_SHARED_TOKEN, and
# TOKEN_PEPPER in the caller's environment.
apiVersion: v1
kind: Secret
metadata:
  name: gateway-system-secrets
  namespace: gateway-system
type: Opaque
stringData:
  admin-token: replace-me
  gateway-token: replace-me
  token-pepper: replace-me
